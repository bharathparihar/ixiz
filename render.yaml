services:
  - type: web
    name: vcards
    env: docker
    plan: free
    region: oregon 
    buildCommand: docker build -t vcards .
    envVars:
      - key: APP_KEY
        value: base64:0hoFoR/0aUmwbl+7bLPJESRwH/WUhKPJapEaEAvDfQ4=
      - key: APP_DEBUG
        value: true
      - key: APP_ENV
        value: production
      - key: DB_CONNECTION
        value: pgsql
      - key: DATABASE_URL
        fromDatabase:
          name: vcard_db
          property: connectionString
      - key: PORT
        value: 80

databases:
  - name: vcard_db
    plan: free
    region: oregon
